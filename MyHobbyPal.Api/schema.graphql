schema{
  query: Query
}

type Query{
  person(id: String!, ): Person!
  persons: [Person!]
}

type Person{
  id: String!
  pk: String!
  label: String!
  givenName: String!
  familyName: String!
  phoneNumbers: [String!]
}
